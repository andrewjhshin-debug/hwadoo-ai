// ────────────────────────────────────────────────────────────────
// 차담회는 손잡고 절로의 '모임'으로 합쳐졌다 — 옛 주소는 그리로 보낸다.
// (북마크·검색으로 들어온 걸음이 끊기지 않게 자리만 남겨 둔다)
// ────────────────────────────────────────────────────────────────

import { redirect } from "next/navigation";

export default function GatheringPage() {
  redirect("/pilgrimage");
}
